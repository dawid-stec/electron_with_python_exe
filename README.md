Tested on Node 22 and Python 3.14  

npm install  
python -m pip install pyinstaller  
npm run build  

After running that in project's directory there will be new directory created named `release`. There will be an installer for app.  
